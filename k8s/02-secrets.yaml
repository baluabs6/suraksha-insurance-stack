# Template only — do not commit real values. Populate these via
# `kubectl create secret` or, better, an External Secrets Operator synced
# from Vault/AWS Secrets Manager/GCP Secret Manager. Every value below is a
# placeholder; nothing here is safe to apply as-is.
apiVersion: v1
kind: Secret
metadata:
  name: suraksha-secrets
  namespace: suraksha
type: Opaque
stringData:
  SPRING_DATASOURCE_USERNAME: "suraksha"
  SPRING_DATASOURCE_PASSWORD: "REPLACE_ME"
  JWT_SECRET: "REPLACE_ME_WITH_A_LONG_RANDOM_VALUE_MIN_32_CHARS"
  ANTHROPIC_API_KEY: "REPLACE_ME"
  POSTGRES_PASSWORD: "REPLACE_ME"
